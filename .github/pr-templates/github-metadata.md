Automated GitHub metadata refresh.

Release summaries are generated by `scripts/summarize_releases.py` and appended to
`src/_data/planet_feeds.json` as `approved: true` — they publish on merge. A summary
that reads wrong is worth fixing in the diff before merging; the source release notes
are linked from each item above.
